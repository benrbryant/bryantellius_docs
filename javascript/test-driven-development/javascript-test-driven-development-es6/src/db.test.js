import { expect } from "chai";
import { getUserByUsername } from "./db";
import { MongoClient } from "mongodb";

describe("getUserByUsername", () => {
  it("gets the correct user from the database given a username", () => {
   
  });
});
